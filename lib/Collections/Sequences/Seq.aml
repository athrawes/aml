module Seq extends Monoid Monad Traversable
  type Seq<'a>

chain = list -> callback ->
  flatten (map list callback)

prepend : 't -> 'm<'t> -> 'm<'t>
  where 'm extends Seq

# An infix alias for `prepend`.
# @include prepend
(::) = prepend
