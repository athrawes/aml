module Effect extends Constructable, Mappable, Applyable, Thenable =
  /** @inheritdoc */
  apply = callback -> value ->
    callback >>= ((swap-args map) value)

  /** @inheritdoc */
  map = callback -> value ->
    chain (callback >> from) value
