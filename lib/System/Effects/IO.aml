Effect = use "System/Control/Effect"

module IO<A> extends Effect =
  | Pending
  | Running
  | Complete<A>

  # @inheritdoc
  from = Complete

  # @inheritdoc
  then = io -> callback ->
    match io
    | Complete [ value ] -> callback value
    | Running -> todo!
    | Pending -> todo!
