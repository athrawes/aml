Effect = use "System/Control/Effect"

type Maybe<A>:
  | Some<A>
  | None

module Maybe :> Effect =
  # @inheritdoc
  from = Some

  # @inheritdoc
  then = maybe -> callback ->
    match maybe
    | Some [ value ] -> callback value
    | None -> None
