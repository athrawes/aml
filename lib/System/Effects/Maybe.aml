Effect = use "System/Control/Effect"

module Maybe<A> extends Effect =
  | Some<A>
  | None

  # @inheritdoc
  from = Some

  # @inheritdoc
  then = maybe -> callback ->
    match maybe
    | Some [ value ] -> callback value
    | None -> None
