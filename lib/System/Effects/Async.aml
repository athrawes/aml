Effect = use "System/Control/Effect"

module Async<A> extends Effect =
  | Pending
  | Resolved<A>

  # @inheritdoc
  from = Resolved

  # @inheritdoc
  then = async -> callback ->
    match async
    | Resolved [ value ] -> callback value
    | Pending -> todo!
