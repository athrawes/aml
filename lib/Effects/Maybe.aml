Effect = use "Control/Effect"

module Maybe :> Effect

type :maybe<'a> =
  | :some<'a>
  | :none

from = :some

then = self callback ->
  match self
  | :some [value] => callback value
  | :none => :none
