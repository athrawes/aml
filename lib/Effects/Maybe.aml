Effect = use "Control/Effect"

module Maybe extends Effect

type Maybe<'a> =
  | Some<'a>
  | None

from = Some

then = maybe callback ->
  match maybe
  | Some (value) => callback value
  | None => None
