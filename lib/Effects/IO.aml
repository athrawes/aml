Effect = use "Control/Effect"

module IO extends Effect

type IO<'a> =
  | Pending
  | Running
  | Complete<'a>

from = Complete

then = io callback -> match io
  | Complete (value) => callback value
  | Running => todo!
  | Pending => todo!
