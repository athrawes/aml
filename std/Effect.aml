let Effect[^M] extend
    Constructable[^M],
    Mappable[^M],
    Applyable[^M],
    Chainable[^M],
    :=

    /** @inheritdoc */
    let apply := fn -> value ->
        fn >>= ((swap-args map) value)

    /** @inheritdoc */
    let map := fn -> value ->
        chain (fn >> from) value
